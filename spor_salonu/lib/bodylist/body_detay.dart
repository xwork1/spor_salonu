import 'package:flutter/material.dart';

class BodyDetay extends StatelessWidget {
  const BodyDetay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Detay Test'),
      ),
      body: Container(),
    );
  }
}
